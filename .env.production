# Production Configuration
# This file is used for production deployment with deployed backend

# Backend URL - Deployed backend
BACKEND_URL=https://syedmuhammadanasmaududi-rag-chabot.hf.space

# Streaming endpoint path
STREAMING_ENDPOINT=/api/chat/stream

# Request timeout (30 seconds)
TIMEOUT_MS=30000

# Retry attempts on network failures
RETRY_ATTEMPTS=3

# Retry delay (exponential backoff starting at 1 second)
RETRY_DELAY_MS=1000

# Message limit before cleanup
MESSAGE_LIMIT=100
